name: appcenter_sdk_plus_example
description: "Demo app for sqlite3_async package."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  logging: ^1.3.0
  logging_appenders: ^2.0.0+1
  appcenter_sdk_plus:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
